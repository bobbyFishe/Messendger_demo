<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Messendger_demo" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Messendger_demo" parent="Base.Theme.Messendger_demo" />

    <style name="CustomPopupMenu" parent="Widget.Material3.PopupMenu">
        <item name="android:popupBackground">@drawable/popup_rounded_bg</item>

        <item name="shapeAppearance">@null</item>
        <item name="backgroundTint">@android:color/transparent</item>

        <item name="android:colorBackground">#648C36</item>
        <!-- Цвет текста и шрифт -->
        <item name="android:textColor">#204E7F</item> <!-- Белый текст -->
        <item name="android:fontFamily">@font/caveat_medium</item>
        <item name="android:textSize">16sp</item>

        <!-- Ссылка на стиль самого "полотна" (для углов) -->
        <item name="android:dropDownListViewStyle">@style/CustomDropDownListView</item>
    </style>

    <!-- 2. Стиль для закругления углов -->
    <style name="CustomDropDownListView" parent="Widget.AppCompat.ListView.DropDown">
        <item name="android:background">@android:color/transparent</item>
        <item name="android:divider">@null</item>
    </style>
</resources>